apiVersion: v1
kind: Service
metadata:
  name: frame-0
  namespace: frame-0
  labels:
    app.kubernetes.io/name: frame-0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frame-0
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
